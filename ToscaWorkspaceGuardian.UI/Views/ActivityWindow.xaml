<Window x:Class="ToscaWorkspaceGuardian.UI.Views.ActivityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Workspace Activity Monitor" Width="760" Height="520" MinWidth="520" MinHeight="360"
        Background="#09121F" Foreground="#EAF2FC" WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <SolidColorBrush x:Key="Muted" Color="#9DB2CD"/>
        <Style TargetType="ScrollViewer"><Setter Property="VerticalScrollBarVisibility" Value="Auto"/></Style>
    </Window.Resources>
    <Grid Margin="22">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="16"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="Workspace Activity Monitor" FontSize="22" FontWeight="SemiBold"/>
            <TextBlock Text="Live, non-sensitive trace of the read-only TCShell workspace scan. Passwords, secrets and command arguments are never shown." Foreground="{StaticResource Muted}" TextWrapping="Wrap" Margin="0,5,0,0"/>
        </StackPanel>
        <Border Grid.Row="2" Background="#0E1B2D" BorderBrush="#29415E" BorderThickness="1" CornerRadius="10" Padding="6">
            <ScrollViewer>
                <ItemsControl ItemsSource="{Binding WorkspaceActivity}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="8,5">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="65"/><ColumnDefinition Width="88"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Timestamp}" Foreground="#7895B8" FontFamily="Consolas"/>
                                <TextBlock Grid.Column="1" Text="{Binding Level}" Foreground="#83C8FC" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="2" Text="{Binding Message}" TextWrapping="Wrap"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
